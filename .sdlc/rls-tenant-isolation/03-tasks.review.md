# Review: 03-tasks.md
VERDICT: RED

1. [blocking] No task converts `services/config/users.py`'s `get_user_by_id` (the design's named `platform_conn(reason="identity-resolution")` bypass shared by 4 identity-resolution callers) or `list_users` (`platform_conn(reason="users-null-tenant-listing")` whenever `tenant_id is None`) — the design's own bypass-sites table (lines ~908-909, ~941-952) specifies both as required conversions, and the inventory table lists `services/config/users.py` at 5 `acquire()` + 6 pool-level sites, but T27 only covers the `PATCH`/`DELETE /users/{user_id}` fetch-then-mutate path via the new `get_user_for_admin`. Contrast with T28, which explicitly says "convert `invites.py`'s remaining acquire/pool-level sites" for the analogous module. Without an equivalent task, `get_user_by_id`/`list_users` stay on bare pool calls, T55's AST tripwire (`test_no_bare_pool_calls.py`, done when "green at zero findings after Phases 4-7") has no task left to make it pass, and post-cutover identity resolution (`GET /auth/me`, `assert_current_authority`, `fresh_authority`, the tenants concurrency route) breaks for every NULL-tenant platform account exactly as lesson 27 already burned this codebase once — fix: add a task in Phase 7 (parallel to T28) — "Convert `users.py`'s remaining acquire/pool-level sites: `get_user_by_id` → `platform_conn(reason=\"identity-resolution\")`, `list_users` → `platform_conn(reason=\"users-null-tenant-listing\")` when its `tenant_id` argument is `None`, else `tenant_conn()`" — done when: module is clean of bare pool calls and the four identity-resolution callers still return NULL-tenant accounts.
