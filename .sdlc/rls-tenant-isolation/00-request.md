Implement Row-Level Security (RLS) in the database for tenant isolation, as a real defense-in-depth layer (not just app-level WHERE tenant_id=$1 checks). Scope confirmed with the user: full enforcement, not just schema-level policies — this requires (1) RLS policies on every tenant-scoped table, (2) a new non-superuser Postgres role for the app to connect as (the current role is a superuser with rolbypassrls=true, so policies alone would be silently inert), and (3) every service (services/config, services/campaigns, services/toolexec, services/knowledge, services/conversation) setting a per-request session variable (e.g. SET LOCAL app.tenant_id) before querying, threaded from each request's already-resolved tenant identity. Must stay on the current branch (redesign) — do not create a new feature branch off main. Deliverable also includes a README documenting exactly what changed and why.
