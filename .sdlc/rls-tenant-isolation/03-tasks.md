# Tasks: Database-Enforced Row-Level Security for Tenant Isolation

## Phase 1: Schema, role and RLS policies

- [x] T1 Create `yuviz_app`/`yuviz_platform` roles, grants and `ALTER DEFAULT PRIVILEGES` — `database/rls.sql` — done when: `psql -f database/rls.sql` run twice against a fresh DB is idempotent, and `pg_roles` shows `yuviz_app` NOLOGIN=false/NOBYPASSRLS and `yuviz_platform` NOLOGIN/BYPASSRLS with `yuviz_app` a member (AC 1).
- [x] T2 Add `audit_log.tenant_id` column + GUC-derived default + index, and its pre-policy backfill `DO` block covering all 18 `entity_type` values — `database/schema.sql`, `database/rls.sql` — done when: column exists nullable, index exists, and re-running the backfill against a DB with pre-existing rows changes nothing on the second run (idempotent), verified against a non-empty fixture per lesson 12/13.
- [x] T3 Add Wave A UUID-keyed `CREATE POLICY`/`ENABLE`/`FORCE` blocks for the 18 UUID tenant-column tables, each in its own atomic `DO $$` block with `CREATE POLICY` before `ENABLE` — `database/rls.sql` — done when: `pg_policies` has 18 rows for these tables and `pg_class.relrowsecurity`/`relforcerowsecurity` are true for each, verified against a DB seeded with pre-existing duplicate-prone data (lesson 13).
- [x] T4 Add Wave A TEXT-slug policy blocks for `calls` and `live_call_interventions` using `app.tenant_slug` — `database/rls.sql` — done when: `pg_policies` shows 2 more rows using the slug GUC, and a manual `SET LOCAL app.tenant_slug` session returns only that tenant's rows.
- [x] T5 Add Wave B parent-join policy blocks for the 10 no-tenant-column child tables — `database/rls.sql` — done when: `pg_policies` count is 30 total, and a manual session scoped to tenant A cannot read a `campaign_contacts`/`custom_api_params`/etc. row whose parent belongs to tenant B.
- [x] T6 Wire `rls.sql` into schema apply order as the 4th file, add `POSTGRES_APP_DSN`/`POSTGRES_ADMIN_DSN` (falling back to `POSTGRES_DSN`), keep every service on the superuser DSN — `deployment/docker/*`, `deployment/sh/dev.sh`, `scripts/start_local.sh` — done when: a fresh `docker compose up` applies `schema.sql`, `knowledge_schema.sql`, `telephony_schema.sql`, `rls.sql` in order and every service still starts (RLS live but inert against the superuser).
- [x] T7 `tests/test_rls_isolation.py` — direct-`yuviz_app` negative suite: cross-tenant read empty, no-GUC read empty (not error), cross-tenant write rejected with `InsufficientPrivilegeError`, `calls` slug path, platform bypass restores today's row set and reverts at transaction end — done when: all cases pass against T1–T5's schema, and the cross-tenant-read case has a same-tenant counter-check that would fail on an empty fixture (AC 3/4/5/6/10, lesson 12).
- [x] T8 `tests/test_rls_coverage.py` part (a) — enumerate every `public` table with a `tenant_id` column, assert RLS+FORCE+≥1 policy; assert the Wave B table list by name — `tests/test_rls_coverage.py` — done when: the test fails if a `tenant_id` column is added with no policy (lesson 12), and passes against T1–T5.

## Phase 2: Shared tenancy library

- [x] T9 `TenantScope`, the `ContextVar`, `set_caller_tenant`/`set_target_tenant`/`current_scope`/`current_tenant` (precedence: `target if caller is None else caller`), `TenantUnresolved`, `TenantScopeConflict` — `libs/tenancy/session.py`, `tests/test_tenant_conn.py` — done when: precedence tests pass in both call orders (`set_caller_tenant`/`set_target_tenant` in either order), and a tenant-scoped caller's `current_tenant()` cannot be moved by any `set_target_tenant` value.
- [x] T10 `tenant_conn()`/`platform_conn()` context managers (one-statement GUC resolver, `explicit_tenant`/`reason=` validation, `stamp_tenant=` on `platform_conn`), `libs/tenancy/__init__.py` re-exports — `libs/tenancy/session.py`, `libs/tenancy/__init__.py`, `tests/test_tenant_conn.py` — done when: pool-reuse-leak test (min_size=1) shows no GUC bleed between requests, fail-closed test raises `TenantUnresolved` before yielding for an empty/unresolvable scope, and `explicit_tenant` without `reason=` raises `ValueError` while a caller-conflicting `explicit_tenant` raises `TenantScopeConflict` before yielding (AC 8/9).

## Phase 3: Identity and shared predicates

- [x] T11 `get_authenticated_user` calls `set_caller_tenant(user.tenant_id)` after decode; add `bind_path_tenant`, `require_path_tenant_access`, `assert_tenant_access` (lifted verbatim from `toolexec/routers/custom_apis.py:38`) — `services/config/deps.py` — done when: a unit test asserts `assert_tenant_access`'s 403 (UUID mismatch) / 404 (slug mismatch or unknown) shapes match `custom_apis.py:38`'s existing shapes exactly, and `bind_path_tenant` raises nothing for any input.

## Phase 4: Tier 2 — tenant-path routers (parallelizable across services once T11 lands)

- [x] T12 Add `bind_path_tenant`+`require_path_tenant_access` to `agents.py:16` and `calls.py:10`; convert `agents.py`'s 3 `acquire()`+3 pool-level and `calls.py`'s 11 pool-level call sites to `tenant_conn()`/`platform_conn()` — `services/config/routers/agents.py`, `services/config/agents.py`, `services/config/calls.py` — done when: `test_rls_coverage.py` part (b) sees both dependencies on these two router declarations, and `test_no_bare_pool_calls.py` reports zero remaining bare pool calls in these two modules.
- [x] T13 Add both dependencies to `carriers.py:11` and `phone_numbers.py:13`; convert their acquire/pool-level sites, including `phone_numbers.py`'s `prewarm()` to `platform_conn(reason=...)` — `services/config/routers/carriers.py`, `services/config/carriers.py`, `services/config/routers/phone_numbers.py`, `services/config/phone_numbers.py` — done when: coverage tripwire passes for both routers and `test_no_bare_pool_calls.py` is clean for both modules.
- [x] T14 Add both dependencies to `provider_configs.py:14`; `_require_tenant_access` becomes an import of the shared `require_path_tenant_access`/`assert_tenant_access`; convert its acquire/pool-level sites — `services/config/routers/provider_configs.py`, `services/config/provider_configs.py` — done when: coverage tripwire passes and no local duplicate of `_require_tenant_access` remains in the file.
- [x] T15 Add both dependencies to `tool_provider_configs.py:11`; convert its acquire/pool-level sites — `services/config/routers/tool_provider_configs.py`, `services/config/tool_provider_configs.py` — done when: coverage tripwire passes and `test_no_bare_pool_calls.py` is clean.
- [x] T16 Add both dependencies to `campaigns/routers/campaigns.py:17` (`/tenants/{tenant_id}/campaigns`) and `:103` (`/tenants/{tenant_id}/dnc`); convert `campaigns.py`'s 2 acquire+4 pool-level and `dnc.py`'s 4 pool-level sites — `services/campaigns/routers/campaigns.py`, `services/campaigns/campaigns.py`, `services/campaigns/dnc.py` — done when: coverage tripwire passes for both router declarations and both service modules are clean of bare pool calls.
- [x] T17 Add both dependencies to `knowledge_bases.py:13`; convert its 3 acquire+3 pool-level sites — `services/knowledge/routers/knowledge_bases.py`, `services/knowledge/knowledge_bases.py` — done when: coverage tripwire passes and the module is clean of bare pool calls.
- [x] T18 Add both dependencies to `custom_apis.py:32`; local `_require_tenant_access` becomes an import of the shared one; convert its 3 acquire+4 pool-level sites — `services/toolexec/routers/custom_apis.py`, `services/toolexec/custom_apis.py` — done when: coverage tripwire passes and no local duplicate helper remains.
- [x] T19 Add both dependencies to `did/routers/numbers.py:34` (previously had no tenant check of any kind); convert `purchased_numbers.py`'s 2 acquire+3 pool-level sites — `services/did/routers/numbers.py`, `services/did/purchased_numbers.py` — done when: a tenant admin of A hitting `POST /tenants/{B}/numbers/purchase` gets 403 with no carrier call made (test 4d), replacing today's unchecked cross-tenant purchase.
- [x] T20 `_resolve_scope` calls `set_target_tenant(tenant["id"])` at its successful return, after its existing 403 branches; convert `live_calls.py`'s 4 acquire sites — `services/config/routers/live_calls.py` — done when: a viewer of A on `GET /tenants/{B-slug}/calls`-equivalent live-calls query param path reads only B's rows as a platform actor and gets today's 403/404 as a tenant-scoped one.
- [x] T21 `tests/test_rls_coverage.py` part (b) — walk `app.routes` on all six apps; assert every route whose path contains `/tenants/{` carries both `bind_path_tenant` and `require_path_tenant_access` in its dependency chain — `tests/test_rls_coverage.py` — done when: the test fails if either dependency is missing from any of the 12 Tier 2 declarations (including `did`), and passes after T12–T20.

## Phase 5: Tier 3 — flat by-id routers

- [x] T22 `telephony_configs` by-id routes gain `platform_scoped` on the (cached) resolver; after the fetch, `deps.assert_tenant_access(row["tenant_id"], current_user)` then `set_target_tenant(row["tenant_id"])`; the mutation runs on plain `tenant_conn()`, reserving `platform_conn(stamp_tenant=row["tenant_id"])` only for `row["tenant_id"] is None` — `services/config/routers/telephony_configs.py`, `services/config/telephony_configs.py` — done when: the fetch and its follow-up mutation demonstrably share one resolved tenant scope (no separate-connection gap — this closes the open medium finding), and the cached-read test (4a, primed then re-read) 404s for a cross-tenant id on both cold and warm cache.
- [x] T23 `carriers.py` and `phone_numbers.py` by-id routes gain `platform_scoped` + post-fetch `assert_tenant_access` (no check today) — `services/config/routers/carriers.py`, `services/config/routers/phone_numbers.py` — done when: AC 6 matrix Tier 3 cases pass for both routers (cross-tenant id → 404, own-tenant id → 200, platform actor → 200 on either).
- [x] T24 `tool_provider_configs.py` by-id routes gain `platform_scoped` + post-fetch `assert_tenant_access` — `services/config/routers/tool_provider_configs.py` — done when: matrix case passes.
- [x] T25 `calls.py`'s `_caller_tenant_slug` narrows its `is_unscoped` predicate to `deps.is_platform_scoped` (lesson 24) and selects `platform_conn`/`tenant_conn` accordingly for the flat `/calls` by-id routes — `services/config/routers/calls.py` — done when: a `role='superadmin'` account with a leftover `tenant_id` gets 403/404 on another tenant's call instead of today's bypass (test 3c-equivalent for Tier 3).
- [x] T26 `agent_tool_policies.py` by-id routes gain `platform_scoped` + post-fetch `assert_tenant_access` (today only `validate_id_exists`) — `services/config/routers/agent_tool_policies.py` — done when: matrix case passes.
- [x] T27 New `users.get_user_for_admin(user_id, *, platform_scoped)`; `PATCH`/`DELETE /users/{user_id}` fetch the row, call `deps.assert_tenant_access(row["tenant_id"], current_user)`, then for a platform-scoped branch mutate under `platform_conn(reason="users-admin-by-id", stamp_tenant=row["tenant_id"])` only when `row["tenant_id"] is None`, otherwise mutate on plain `tenant_conn()`; `PATCH` additionally asserts the new `tenant_id` before writing — `services/config/routers/users.py`, `services/config/users.py` — done when: self-promotion (`{"tenant_id": null}`) is 403 for a tenant-scoped actor and 200 for a platform-scoped one, cross-tenant edit/delete is 404 with the target row unmodified, cross-tenant move is 403, and the fetch/mutation pair share one resolved scope (closes the open medium finding for this router).
- [x] T28 `POST /invites` becomes Tier 4 (`assert_tenant_access(body.tenant_id, current_user)` then `set_target_tenant(body.tenant_id)`, existing `may_invite`/`_same_tenant` kept); `resend`/`revoke` become Tier 3 by-id with `platform_scoped` + `assert_tenant_access`; convert `invites.py`'s remaining acquire/pool-level sites — `services/config/routers/invites.py`, `services/config/invites.py` — done when: all four Tier 4 combinations (platform→tenant, platform→NULL, tenant→own, tenant→other) match the design's specified status codes.
- [x] T29 `provider_configs.py` by-id routes' existing `_authorize_provider` gains `platform_scoped` (cached resolver) — `services/config/routers/provider_configs.py` — done when: cached-read test (4a, primed) 404s for a cross-tenant id.
- [x] T30 `campaigns`/`dnc` 9 flat by-id routes gain `platform_scoped` + post-fetch `assert_tenant_access` — `services/campaigns/routers/campaigns.py`, `services/campaigns/campaigns.py`, `services/campaigns/dnc.py` — done when: matrix passes for all 9 declarations.
- [x] T31 `knowledge_bases.py`'s existing `_authorize_kb` gains `platform_scoped` — `services/knowledge/routers/knowledge_bases.py` — done when: matrix case passes.
- [x] T32 `documents.py` and `agent_kb.py` by-id routes gain `platform_scoped` + post-fetch `assert_tenant_access` (no check today); convert `agent_kb.py`'s 8 pool-level sites — `services/knowledge/routers/documents.py`, `services/knowledge/routers/agent_kb.py`, `services/knowledge/agent_kb.py` — done when: matrix passes for both routers and `agent_kb.py` is clean of bare pool calls.
- [x] T33 `custom_apis.py`'s existing `_authorize_custom_api` gains `platform_scoped` — `services/toolexec/routers/custom_apis.py` — done when: matrix case passes.
- [x] T34 `agent_apis.py`'s existing `tenant_filter` (`:66,182`) gains `platform_scoped`; `tenant_filter is None` branch selects `platform_conn`; convert its remaining 2 acquire+5 pool-level sites — `services/toolexec/agent_apis.py`, `services/toolexec/routers/agent_apis.py` — done when: matrix case passes for both by-id routes.
- [x] T35 `did`'s `/numbers/{id}/assign|release` gain `platform_scoped` + post-fetch `assert_tenant_access` (no check today) — `services/did/routers/numbers.py` — done when: matrix case passes.
- [x] T36 `tests/test_rls_coverage.py` part (c) — for every non-`/tenants/{` route with a `{...}` id segment, assert its module is on the Tier 3 table and calls `assert_tenant_access`; Tier 4 routes exempted by name and asserted to call `set_target_tenant` instead — `tests/test_rls_coverage.py` — done when: the test fails if a new flat by-id route is added without either call, and passes after T22–T35.
- [x] T37 `tests/test_cross_tenant_admin.py` — the `users` module cases: self-promotion refused with platform-scoped counter-arm, cross-tenant edit/delete refused, cross-tenant move refused with a DB-layer `InsufficientPrivilegeError` counter-check, identity resolution (`GET /auth/me`, live-calls intervention, tenant concurrency route) unaffected, `GET /users` for a NULL-tenant viewer service account unchanged — `tests/test_cross_tenant_admin.py` — done when: all five cases pass against T27.
- [x] T38 `tests/test_cross_tenant_admin.py` — Tier 3 matrix parameterised over all 17 flat routers (platform actor sees the row, tenant actor 404/403s on a foreign id, tenant actor 200s on its own), plus cached-read case 4a primed on both `telephony_configs` and `provider_configs` — `tests/test_cross_tenant_admin.py` — done when: all 17 routers pass both directions, and the cached-priming case would fail without T22/T29's `platform_scoped` fix (state what makes it fail, lesson 12).

## Phase 6: Tier 4 — service-account body-tenant routes

- [x] T39 After `require_execute_subject` and the existing body/identity check, call `set_target_tenant(body.tenant_id)`; convert `executor.py`'s 11 pool-level sites — `services/toolexec/routers/execute.py`, `services/toolexec/executor.py` — done when: a non-allow-listed service account still 403s (`require_execute_subject` unchanged), and Conversation's call writes `api_chain_runs` with the body's `tenant_id`.
- [x] T40 `POST /internal/retrieve` and `GET /internal/agents/{tenant_slug}/…/has-knowledge` call `deps.assert_tenant_access(<body or path tenant>, current_user)` then `set_target_tenant(...)` (retrieve.py was gated on bare `get_current_user` today — this is a new tightening); `retrieval.py`/`vector_repository.py` take a connection argument instead of the pool, opened by this router — `services/knowledge/routers/retrieve.py`, `services/knowledge/retrieval.py`, `services/knowledge/vector_repository.py` — done when: a tenant-scoped account gets 403 on `/internal/retrieve` for another tenant's slug, Conversation's NULL-tenant service account still gets B's context with the GUC set to B, and `has-knowledge` 404s for a foreign slug.
- [x] T41 `tests/test_cross_tenant_admin.py` — Tier 4 cases (4c): `/internal/retrieve`, `/internal/chains/execute`, `has-knowledge`, parameterised by the three named sites — `tests/test_cross_tenant_admin.py` — done when: all three sites' specified status codes and GUC values pass, run against T39–T40.

## Phase 7: Remaining call-site conversions

- [x] T42 Convert `tenants.py`'s 3 acquire+4 pool-level sites to `platform_conn()` (out of RLS scope; NULL-tenant reads still need a resolving connection) — `services/config/tenants.py` — done when: `test_no_bare_pool_calls.py` is clean for this module and its by-id routes (out of Tier 3 by design) are unaffected.
- [x] T43 Convert `workflows.py`'s 1 acquire+7 pool-level sites (reached under Tier 2 `agents` routes, so scope comes from the ambient GUC) — `services/config/workflows.py` — done when: module is clean of bare pool calls and existing agent-workflow tests pass under `yuviz_app`.
- [x] T44 Fix `list_audit_log`'s cross-tenant leak — give it `tenant_id: str | None` + `platform_scoped: bool = False` params (the `users.py:55 list_users` convention); the route derives both from `deps.is_platform_scoped(current_user)` (tenant-scoped → the caller's own `tenant_id`, platform-scoped → `None`); the scoped branch adds an explicit `AND tenant_id = $n` predicate and opens `tenant_conn()`, the platform branch opens `platform_conn(reason="audit-log-platform-read")` with no tenant predicate — `services/config/audit.py`, `services/config/routers/audit_log.py` — done when: a tenant-scoped superadmin's `GET /audit-log` returns only rows with their own `tenant_id` (a cross-tenant fixture row is absent, exercised against a DB seeded with rows for two tenants — not an empty fixture, lesson 12), and a platform-scoped caller still sees every tenant's rows exactly as today.
- [x] T45 Convert `agent_tool_policies.py`'s 3 acquire+2 pool-level sites — `services/config/agent_tool_policies.py` — done when: module is clean of bare pool calls.
- [x] T46 Convert `campaign_contacts.py`'s 1 acquire+4 pool-level sites (Wave B child table) and `campaigns/audit.py` to the mutation's-own-connection rule — `services/campaigns/campaign_contacts.py`, `services/campaigns/audit.py` — done when: both modules are clean of bare pool calls.
- [x] T47 Convert `worker.py`'s 1 pool-level due-campaign scan to `platform_conn(reason="campaign-worker-scan")`; keep the poll loop outside the context manager — `services/campaigns/worker.py` — done when: a test asserting `pg_stat_activity.state` shows no open transaction between scan iterations passes (same automated-assertion shape as T50, not manual inspection).
- [x] T48 Convert `admission.py`'s 2 acquire sites, `audit.py`'s pool-level sites (mutation's-own-connection rule) and `auth_schemes.py`'s acquire sites — `services/toolexec/admission.py`, `services/toolexec/audit.py`, `services/toolexec/auth_schemes.py` — done when: all three modules are clean of bare pool calls and the existing OAuth2 token cache key (already tenant-prefixed) is unchanged. NOTE: verified against actual code — `admission.py` is a pure in-process rate limiter (its own `acquire`/`release`, no DB access at all; the design's inventory count of "2 acquire" conflates this with `pool.acquire()`), `auth_schemes.py` has no DB access either, and `audit.py`'s `write_audit` already takes `conn` as a parameter (mutation's-own-connection rule already satisfied). No code change was needed or made; acceptance criteria hold as-is (grep confirms zero bare pool calls in all three, OAuth2 cache key untouched).
- [x] T49 Convert `retrieval_policies.py`'s 1 acquire site and `knowledge/audit.py`'s pool-level sites (mutation's-own-connection rule) — `services/knowledge/retrieval_policies.py`, `services/knowledge/audit.py` — done when: both modules are clean of bare pool calls.
- [x] T50 Convert `ingestion_worker.py`'s 1 acquire+7 pool-level sites to `platform_conn()` per job, keeping the poll loop outside the context manager (job claim is cross-tenant, per-job work re-scopes to the job's own tenant) — `services/knowledge/ingestion_worker.py` — done when: `pg_stat_activity.state` shows no open transaction between job claims, verified by a test asserting exactly this.
- [x] T51 Convert `did/carriers.py`'s 1 pool-level site and `did/audit.py`'s pool-level sites — `services/did/carriers.py`, `services/did/audit.py` — done when: both modules are clean of bare pool calls.
- [x] T52 Cache the call's tenant slug per `session_id` in `TranscriptBuilder`; per-session writes use `tenant_conn(explicit_tenant=<slug>, reason="conversation-session-write")`; the inactive-call reconcile sweep uses `platform_conn(reason="conversation-reconcile-sweep")`; convert its 5 acquire+4 pool-level sites — `services/conversation/transcript_builder.py` — done when: a per-session write scoped to tenant A cannot read/write tenant B's `calls`/`transcript_entries` row, the sweep still reconciles across tenants, and a test asserting `pg_stat_activity.state` shows no open transaction between sweep iterations passes (same automated-assertion shape as T50, not manual inspection).
- [x] T53 Add `tenant_slug` to `ToolPolicyResolver`'s resolve entry point and its cache key; both acquire sites use `tenant_conn(explicit_tenant=<slug>, reason="conversation-tool-policy")` — `services/conversation/tools/policy_resolver.py` — done when: a resolve call for tenant A cannot return tenant B's `custom_apis`/`agent_custom_apis` rows, and the degraded fallback (`agent_config.py:169`, empty slug) raises `TenantUnresolved` rather than writing under the wrong tenant.
- [x] T54 Point `create_superadmin.py`, `create_service_account.py`, `seed_default_config.py` at `POSTGRES_ADMIN_DSN` (falling back to `POSTGRES_DSN`) — `scripts/create_superadmin.py`, `scripts/create_service_account.py`, `scripts/seed_default_config.py` — done when: all three still write `tenant_id IS NULL` rows successfully against the superuser DSN with `rls.sql` applied.

## Phase 8: Cross-cutting tripwires and regression

- [x] T55 `tests/test_no_bare_pool_calls.py` — AST walk of every module under `services/` (excluding `*/tests/*`), failing on any `.fetch/.fetchrow/.fetchval/.execute/.executemany` call on a `Pool`-typed/`get_pool()`-derived name — `tests/test_no_bare_pool_calls.py` — done when: the suite is green at zero findings after Phases 4–7, and a deliberately reintroduced bare `pool.fetchrow(...)` in a scratch module trips it.
- [x] T56 `tests/test_rls_coverage.py` parts (d)/(f) — collect every `reason=` literal passed to `tenant_conn`/`platform_conn` across `services/` and assert the set equals the union of the bypass and explicit-override tables; assert every `platform_conn` call inside a function that also mutates passes `stamp_tenant=`, exempting the named genuinely-tenantless reasons — `tests/test_rls_coverage.py` — done when: an unlisted `reason=` literal or an un-stamped platform-branch mutation fails the test, verified by deliberately adding one in a scratch site and observing the failure.
- [x] T57 `tests/test_rls_coverage.py` part (e) — assert each of the four `cache._cache_key` builders produces a UUID-bearing or tenant-prefixed key — `tests/test_rls_coverage.py` — done when: a scratch cache key built from a bare `slug`/`name`/`email` fails the assertion.
- [x] T58 `tests/test_cross_tenant_admin.py` — full AC 6 matrix cases 1–3c and 5 (superadmin cross-tenant read/write parity with the superuser baseline, tenant-admin 403/404 on read and write, RLS-alone with `require_path_tenant_access` monkeypatched to a no-op, leftover-`tenant_id` superadmin narrowing, negative control removing `bind_path_tenant`/`require_path_tenant_access` in-test) — `tests/test_cross_tenant_admin.py` — done when: all cases pass against Phase 4's routers and case 3b fails if the `target`-wins precedence were restored (proving RLS is an independent layer).
- [x] T59 Run all six services' existing test suites, unmodified, against a database with `yuviz_app` as the connecting role and `rls.sql` applied (AC 7) — CI config — done when: every existing suite is green with no workaround added to any test; a suite requiring a workaround is filed as a finding, not merged as a pass. NOTE: not green — filed as a finding, `.sdlc/rls-tenant-isolation/04-t59-test-suite-finding.md`. Root cause is one thing (test fixtures/direct service calls that never run through an HTTP request, so no ambient tenant scope is ever set), surfacing as two symptoms depending on whether the untouched test code happens to route through `tenant_conn`/`platform_conn` (`TenantUnresolved`, pre-existing even under the superuser DSN) or bypasses it with a raw `pool.execute`/`fetchrow` (newly-visible `InsufficientPrivilegeError`/RLS-policy-violation under `yuviz_app`, silently masked by `BYPASSRLS` before). All of this feature's own RLS-owning suites (`test_rls_isolation.py`, `test_rls_coverage.py`, `test_cross_tenant_admin.py`, `test_tenant_conn.py`, `test_no_bare_pool_calls.py` — 60 tests) are green against `yuviz_app`; no production defect found.
## Phase 9: Rollout

- [x] T60 Run staging for one deploy cycle on the wiring-only state (services still on the superuser DSN); add the three staging-only log lines in `tenant_conn` (cross-tenant admin path, unresolved-scope WARNING, caller-target-mismatch WARNING) and compare observed `platform_conn` bypasses against the intended list — `libs/tenancy/session.py` (staging log lines), staging deploy — done when: zero unresolved-scope WARNINGs accumulate over a full deploy cycle and the observed bypass set matches the intended-bypass table exactly. NOTE: this sandbox has no staging environment. The two new log lines landed in `libs/tenancy/session.py` (the bypass line already existed); the staging deploy cycle and its log comparison are written up as a runbook for a real operator in `.sdlc/rls-tenant-isolation/05-rollout-runbook.md` — not executed, and not claimed as executed.
- [x] T61 Run the pre-cutover account sweep query and NULL out `tenant_id` on every leftover-default `role='superadmin'`/`is_service_account` row with a non-NULL `tenant_id` — one-time operator script/runbook — done when: the sweep query returns zero unreviewed rows before step T62 proceeds (blocking gate). NOTE: script at `scripts/pre_cutover_account_sweep.sql`. Ran the read-only SELECT against this sandbox's dev DB for real (6 rows, all identifiable leftover test fixtures, not real accounts) to confirm the query is correct; the per-row human disposition (NULL out vs. leave) is documented in the script's comments for a real operator to apply against real data — see `.sdlc/rls-tenant-isolation/05-rollout-runbook.md`.
- [ ] T62 Flip `POSTGRES_DSN` to the `yuviz_app` DSN per service, in order did → campaigns → toolexec → knowledge → conversation → config — `deployment/.env` (per service) — done when: each service starts cleanly on `yuviz_app` and its own suite (T59) and the manual UI walkthrough (superadmin-on-another-tenant first) both pass before the next service flips; rollback is reverting that service's DSN alone. NOT DONE: T59's finding means the per-service "its own suite passes" gate is not honestly met today (real, unfixed test-fixture debt, not a production bug) and this sandbox has no independently-running staging services to cut over one at a time. Left unchecked rather than flip any DSN and claim the gate passed; the precise procedure for a real operator is written up in `.sdlc/rls-tenant-isolation/05-rollout-runbook.md`.

## Phase 10: Documentation

- [x] T63 Write the RLS README as a checklist: role per service, the four tiers, exact wiring location per service, the table list (Wave A/B/Out), how `SET LOCAL ROLE` bypass works, the explicit-override list, the cache-key rule and the "RLS is not a control for any cached value" statement, and what a new table/tenant-path router/flat by-id route/body-tenant route/service/pool/cache must do — `docs/rls-tenant-isolation.md` — done when: every table and rule in it is cross-checked against the actual code landed in Phases 1–9, not against this design document's plan. Also documents the two extra bugs found and fixed by manual verification beyond T1–T58 (`_split_tenant`/`assert_tenant_access` UUID handling; `audit_log.tenant_id`'s FK changed to `ON DELETE SET NULL`) and the one extra authorization gap closed the same way (`knowledge/routers/retrieval_policies.py` GET+PUT and `agent_kb.py`'s GET list route had no tenant check at all).
- [x] T64 Update the schema apply order and multi-tenant model section (caller-vs-target rule, the four tiers, "never call `pool.fetch*` directly") — `CURSOR.md` — done when: the section reflects the shipped tiers and helper names exactly as implemented.
