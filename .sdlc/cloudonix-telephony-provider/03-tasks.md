# Tasks: Cloudonix webhook + Media Stream bridge

## Phase 1: Media Stream SDK extraction (Vobiz stays live throughout)

- [x] T1 Move `services/vobiz/audio.py` verbatim into `libs/media_stream_sdk/audio.py` (rename `vobiz_to_pcm16`→`to_pcm16`, `pcm16_to_vobiz_bytes`→`from_pcm16`, drop unused `pcm16_to_vobiz()`), delete the old file, and add `libs/media_stream_sdk/__init__.py` — `libs/media_stream_sdk/audio.py`, `services/vobiz/audio.py` — done when: `services/vobiz`'s existing test suite passes importing the codec from the new location and `services/vobiz/audio.py` no longer exists.
- [x] T2 Add `libs/media_stream_sdk/serializers.py` with the `MediaStreamSerializer` Protocol, `VobizSerializer`, and `CloudonixSerializer` — `libs/media_stream_sdk/serializers.py`, `libs/media_stream_sdk/tests/test_serializers.py` — done when: `test_serializers.py` passes, with `VobizSerializer.play_frame`/`clear_playback` parsing back to the exact dicts hardcoded in today's `bridge.py:377,463-471`, and `CloudonixSerializer` correctly classifying a Twilio-shaped `start`/`media`/`dtmf`/`stop`/`connected` sample (`connected` → `"other"`).
- [x] T3 Add `libs/media_stream_sdk/bridge.py` (`MediaStreamBridge`, moved verbatim from `services/vobiz/bridge.py` with the four listed edits plus opt-in `MEDIA_STREAM_DUMP_DIR` WAV dump), then reduce `services/vobiz/bridge.py` to `class VobizCallBridge(MediaStreamBridge)` passing `VobizSerializer()` and keeping the `call_uuid=` keyword and `vobiz.bridge` logger name — `libs/media_stream_sdk/bridge.py`, `services/vobiz/bridge.py` — done when: `services/vobiz/tests/test_bridge_dtmf.py` passes unchanged against `VobizCallBridge`.

## Phase 2: Shared libs (rate limiting, DID routing, Cloudonix provider)

- [x] T4 Move `FixedWindowCounter` verbatim out of `services/config/app.py` into `libs/ratelimit.py`, re-exporting it at module scope in `services/config/app.py` — `libs/ratelimit.py`, `services/config/app.py` — done when: `services/config/tests/test_throttle.py` passes unchanged.
- [x] T5 Move `services/vobiz/redis_route.py` into `libs/telephony_sdk/did_route.py`, adding the bounded `asyncio.wait_for` timeout, `resolve_did_route()` (returns `tuple[str,str] | None`, `None` on any miss/malformed/timeout/error), and the `resolve_did()` back-compat wrapper; delete `services/vobiz/redis_route.py` and update `services/vobiz/app.py`'s import and call site — `libs/telephony_sdk/did_route.py`, `services/vobiz/app.py`, `libs/telephony_sdk/tests/test_did_route.py` — done when: `test_did_route.py` passes for hit/miss/malformed JSON/`RedisError`/timeout/post-timeout-recovery, `resolve_did` still returns `("default","default")` on every `None` case, and `services/vobiz/app.py` no longer imports `redis_route`.
- [x] T6 Add `libs/telephony_sdk/providers/cloudonix.py` (`CloudonixProvider`: `required_credential_fields`, `validate_credentials`, `build_answer_response`, `verify_webhook_signature` always `False`; `initiate_call`/`hangup_call`/`get_call_status` raise `TelephonyProviderError`), registered beside `vobiz` — `libs/telephony_sdk/providers/cloudonix.py`, `libs/telephony_sdk/tests/test_did_route.py` (provider tests) — done when: `validate_credentials` rejects an empty `api_keys`, a missing `domain`, `"env:PATH"`, `"k8s:/etc/passwd"`, and a raw key while accepting an `enc:` token; `build_answer_response` returns the exact `<Connect><Stream url=.../></Connect>` XML via `quoteattr`; `verify_webhook_signature` returns `False` even for a valid key.

## Phase 3: Config Service changes

- [x] T7 In `services/config/telephony_configs.py`, add `list_configs_by_provider(provider)` (opened with `platform_conn(reason="cloudonix account preload: cross-tenant by construction")`) and, in `create_telephony_config`/`update_telephony_config`, normalize `provider='cloudonix'` `credentials.api_keys` on write (plaintext → `encrypt_secret`, existing `enc:` kept, anything else including `env:`/`k8s:` → `ValueError`/`400`) — `services/config/telephony_configs.py`, `services/config/tests/test_telephony_config_listing.py` (write-path tests) — done when: creating a `provider='cloudonix'` config with `api_keys=["env:POSTGRES_DSN"]` or `["k8s:/etc/passwd"]` returns `400` and writes no row, and a plaintext key is stored `enc:`-encrypted (not equal to the plaintext).
- [x] T8 Add `GET /telephony-configs?provider=` to `services/config/routers/telephony_configs.py`, gated on `is_platform_scoped(current_user)` (`tenant_id is None`), returning `credentials.api_keys` as sealed `enc:` tokens and `credentials.domain` — `services/config/routers/telephony_configs.py`, `services/config/tests/test_telephony_config_listing.py` — done when: a platform service account (`tenant_id is None`, `role="viewer"`) gets `200`, a tenant admin of the owning tenant gets `403`, and the response body carries no plaintext or `env:`/`k8s:` value.

## Phase 4: Cloudonix service

- [x] T9 Create `services/cloudonix/accounts.py` (`Account` dataclass, `AccountStore` with cold-path preload + `CLOUDONIX_REFRESH_S` periodic refresh via the Config Service login/401-retry helper, last-known-good retention on failed refresh, `_decrypt()` accepting only `is_encrypted()` entries via `decrypt_secret` — never `CompositeSecretResolver`) — `services/cloudonix/accounts.py`, `services/cloudonix/tests/test_admission.py` (AccountStore section) — done when: a refresh with a mix of `enc:` and non-`enc:` `api_keys` entries decrypts only the `enc:` ones and logs+skips the rest, an account left with zero usable keys is dropped from the map, and a failed refresh keeps the previously loaded map (`loaded` stays `True`).
- [x] T10 Create `services/cloudonix/handoff.py` (`CallRoute` dataclass, `HandoffStore` with `issue`/`claim`, 60s TTL, `_MAX_PENDING=10_000` cap raising `HandoffCapacityError`) — `services/cloudonix/handoff.py`, `services/cloudonix/tests/test_admission.py` (handoff section) — done when: `claim()` called twice on the same token returns the route once then `None`, a token past TTL returns `None`, and issuing past `_MAX_PENDING` raises `HandoffCapacityError`.
- [x] T11 Create `services/cloudonix/app.py`'s `GET|POST /cloudonix/voice/{config_id}` webhook implementing the strict 6-step order (not-loaded check, API-key auth via `hmac.compare_digest`, domain check, per-DID/per-account rate limits, `resolve_did_route` + tenant-boundary outcome table, handoff issue + CXML response) — `services/cloudonix/app.py`, `services/cloudonix/tests/test_admission.py` — done when: the R2-1 foreign-DID case returns `403` with no token issued while the same-tenant case returns `200` routed correctly; unknown `config_id`, wrong key, and missing key all return byte-identical `403` bodies with zero Redis calls; Redis miss/`RedisError`/timeout all return `200` CXML routed to `(account.tenant_slug, "default")`; `CLOUDONIX_DID_LIMIT+1` requests for one DID yield `429` on the last one without blocking a different DID on the same account; and `accounts.loaded == False` yields `503` with no CXML.
- [x] T12 Add `WS /cloudonix/stream/{token}` to `services/cloudonix/app.py`, claiming the `HandoffStore` token (unknown token → `close(code=1008)`, no fallback session) and constructing `MediaStreamBridge(serializer=CloudonixSerializer(), ...)` from the claimed `CallRoute` — `services/cloudonix/app.py`, `services/cloudonix/tests/test_bridge_session.py` — done when: a `start` frame produces one `SessionOpenRequest` with `tenant_id`/`script_id` taken from the claimed route (not the URL); a known mulaw `media` frame decodes to the expected PCM waveform; a `dtmf` frame emits a `DtmfDigit` with the digit absent from caplog; and `stop`/abrupt WS close cancels the call and all five bridge tasks.
- [x] T13 Add `services/cloudonix/__init__.py` and `services/cloudonix/__main__.py` (uvicorn entrypoint, default port 8700) plus `GET /health` reporting `AccountStore.loaded` — `services/cloudonix/__init__.py`, `services/cloudonix/__main__.py`, `services/cloudonix/app.py` — done when: `python -m services.cloudonix` starts the app on port 8700 by default and `GET /health` returns the current `loaded` state.

## Phase 5: Deployment, docs, and live verification

- [x] T14 Add a `cloudonix` service entry to `deployment/docker/docker-compose.yml` published as `${CLOUDONIX_BIND_ADDR:-127.0.0.1}:${CLOUDONIX_PORT:-8700}:8700`, never the shared `BIND_ADDR`, with a comment pointing at `docs/cloudonix.md` — `deployment/docker/docker-compose.yml` — done when: `docker compose config` renders the `cloudonix` service with its own bind/port variables and no reference to `BIND_ADDR`.
- [x] T15 Write `docs/cloudonix.md` (setup steps, one-Voice-Application-per-tenant rule, exposure model incl. CIDR allowlist and ingress rate limit, key rotation ownership/procedure, env var table, and an AC12/13/14 writeup section with OQ1-4 placeholders) and add the Cloudonix row/`media_stream_sdk` entry to `CURSOR.md` — `docs/cloudonix.md`, `CURSOR.md` — done when: `docs/cloudonix.md` exists covering every item above and `CURSOR.md`'s area table lists `services/cloudonix/` and its `libs/` line includes `media_stream_sdk`.
- [ ] T16 Perform the live trial-call verification and record results in `docs/cloudonix.md` — `docs/cloudonix.md` — done when: the AC12/13/14 section contains a pass/fail verdict, transcript, and WAV reference for one real inbound call with `MEDIA_STREAM_DUMP_DIR` set, the measured webhook answer budget and final `DID_REDIS_TIMEOUT_MS`, the observed `CallSid`/`Domain` field shapes (OQ2/OQ3), observed inbound concurrency and final rate-limit values (OQ4), an explicit warm-vs-cold transfer finding, the observed mid-call bridge-kill behavior (AC14), and confirmation that one live Vobiz call was re-run with no barge-in regression.
