Make Knowledge Bases a first-class, tenant-level feature instead of something only reachable from inside a single agent's page. Requirements:

1. New top-level "Knowledge bases" nav item + page (tenant-scoped, alongside the existing "AI agents" section), listing all knowledge bases for the tenant as sources: name, type (currently: file upload), status, document/chunk counts, and which agents currently use each one.
2. A redesigned "Add source" modal for creating/uploading into a knowledge base, styled with a 4-option picker (Upload files / Website crawl / Database or warehouse / SaaS connector) plus a drag-and-drop dropzone — but only "Upload files" is actually wired up in this iteration; the other three render as visibly disabled "coming soon" cards since there is no backend for crawl/DB/SaaS ingestion yet. Widen accepted file types beyond the current .txt/.md restriction to whatever services/knowledge already parses (check backend before promising types like PDF/DOCX).
3. Preserve and surface the existing many-to-many relationship: one knowledge base can already be attached to multiple agents (assignKnowledgeBase/listAgentKnowledgeBases already support this) — the new tenant-level page should make that reuse visible and manageable (attach/detach agents from a KB's detail view), not just attach-from-the-agent-side like today.
4. Do NOT touch or merge in API/tool connections — those stay a separate, existing per-agent tab for now. (A similarly reusable tenant-level "API connections" page was mentioned by the user as a future ask, not part of this feature.)

This is a UI/UX-first feature; keep backend changes minimal (reuse existing knowledgeApi.ts endpoints where possible, only add what's needed to list "used by agents" per KB if that's not already exposed by an existing endpoint).
