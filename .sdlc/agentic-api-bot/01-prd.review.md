# Review: 01-prd.md (Agentic API Task Execution)
VERDICT: AMBER

1. [blocking] AC 1 was fixed to clarify the "exactly two tools" surface is scoped to knowledge_search/execute_api only and is additive to legacy hardcoded tools (book_appointment, cancel_appointment, reschedule_appointment, send_sms), but the Scope section (line 9: "Exactly two tools are ever exposed to the LLM, regardless of how many APIs a tenant has registered: `knowledge_search`... and `execute_api`") still states the old, unqualified claim, as does the Open Questions summary (line 53: "tool surface is exactly `knowledge_search` and `execute_api`"). An agent with book_appointment/send_sms enabled plus a custom API still sees more than two tool schema entries, so Scope and Open Questions now contradict the corrected AC 1 — a reader of Scope alone (which is where an architect starts) will still build the wrong tool surface. Fix: reword Scope line 9 and the Open Questions line 53 to match AC 1's "exactly one additional entry (execute_api) for all custom APIs, additive to legacy hardcoded tools" framing.

Everything else verified unchanged and intact from the prior pass: all three open questions genuinely closed, ACs 3/4/11/13 correctly reworded with no leftover pending tags, AC 15 idempotency and the Constraints redaction bullet (line 44) both survived, AC count remains 17, no worktree remark leaked into the ACs, and all cited files/symbols (`services/conversation/tools/registry.py`, `orchestrator.py`, `types.py`, `policy_resolver.py`, `pipeline.py`'s `_claims_booking_without_tool_call`, `database/schema.sql`'s `tool_provider_configs`/`agent_tool_policies`, `services/config/auth.py`'s `CurrentUser`, `admin-ui/app/agents/[tenantSlug]/[agentSlug]/page.tsx`'s `Tab` union, `admin-ui/components/KnowledgeBasePanel.tsx`, `.claude/sdlc/lessons.md` #3/#4) still exist as described.
