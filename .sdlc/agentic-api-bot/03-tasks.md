# Tasks: Agentic API Task Execution (Multi-Level Tool Chains)

## Phase 1: Schema
- [x] T1 Append `custom_apis`, `custom_api_params`, `agent_custom_apis`, `api_chain_runs`, `api_chain_steps`, `api_side_effect_claims` and `agent_tool_policies.max_chain_depth` to `database/schema.sql`, all as additive `IF NOT EXISTS`/`ADD COLUMN IF NOT EXISTS` statements — `database/schema.sql` — done when: `psql -f database/schema.sql` applies cleanly twice in a row against a fresh DB (idempotent), and all six new relations plus the new column exist. Resolve the `rate_limited` status loose end explicitly: since admission (executor step 2b) rejects before any `api_chain_runs` row is ever created, drop `'rate_limited'` from the `api_chain_runs.status` CHECK — it stays only as a `ChainExecuteResponse.chain_status` literal — done when: an `INSERT ... status='rate_limited'` into `api_chain_runs` violates the CHECK constraint.

## Phase 2: Tool Execution Service skeleton
- [x] T2 Create `services/toolexec/__init__.py`, `__main__.py`, `app.py`, `db.py` — FastAPI app with `lifespan` eager pool connect/`close_pool`, CORS for `localhost:3000`, `LookupError`→404 / `ValueError`→400 / FK-violation→400 handlers, `/health`, `uvicorn` entry on port 8600 — `services/toolexec/app.py`, `services/toolexec/db.py`, `services/toolexec/__main__.py` — done when: `GET /health` returns `{"status":"ok"}` against a running instance, mirroring `services/knowledge/app.py`'s shape.
- [x] T3 Add `services/toolexec/audit.py` (`write_audit()` into shared `audit_log`, `entity_type='custom_api'`) and `services/toolexec/schemas.py` (Pydantic request/response models: `CustomApiCreate/Update`, `CustomApiParamSpec`, `ChainExecuteRequest/Response`, `ChainStepReport`) — `services/toolexec/audit.py`, `services/toolexec/schemas.py` — done when: importing both modules succeeds and `ChainExecuteRequest`/`ChainExecuteResponse` round-trip a sample payload through `model_validate`/`model_dump`.

## Phase 3: Tenant-authored input validation (security-critical)
- [x] T4 Implement `services/toolexec/auth_schemes.py` `validate_tenant_ref()` and `resolve_tenant_ref()` — confines a tenant-authored credential ref to `enc:` (always), `env:TENANT_<this tenant's uuid hex>_*`, or `k8s:tenants/<tenant_id>/<name>` under `TOOLEXEC_TENANT_SECRET_ROOT` with a `.resolve()`/`relative_to()` containment check; re-validated at resolution from the row's own `tenant_id`, never only at registration — `services/toolexec/auth_schemes.py`, `services/toolexec/tests/test_auth_schemes.py` — done when: `test_auth_schemes.py` passes, including the namespace-containment cases (`env:JWT_SECRET`, `env:SECRET_ENCRYPTION_KEY`, `env:TENANT_<other tenant's hex>_TOKEN`, `k8s:../../etc/passwd`, `k8s:tenants/<other tenant_id>/token`, and the symlink-escape case) each asserting `credential_ref_outside_tenant_namespace` **and** that the sentinel platform secret was never read.
- [x] T5 Implement `services/toolexec/custom_apis.py` `resolve_and_validate_endpoint()` (SSRF guard: https-only unless host-allow-listed, no userinfo/fragment/non-default port, every A/AAAA record checked via `ipaddress` against the deny-list, reject whole-URL on any bad record) — `services/toolexec/custom_apis.py`, `services/toolexec/tests/test_endpoint_validation.py` — done when: `test_endpoint_validation.py` passes, including `169.254.169.254`, `::1`, `::ffff:127.0.0.1`, decimal/hex IPv4 forms, the multi-A-record-one-private case (whole URL rejected, not "the good one" dialed), and userinfo rejection; accepts a normal public host.
- [x] T6 Implement `services/toolexec/graph.py` — `MAX_CHAIN_LEVELS = 4`, `resolve_order(api, max_levels)` (post-order, dedup'd, `depth_limit_exceeded`/`cycle_detected`), `extract(response, json_path)` — `services/toolexec/graph.py`, `services/toolexec/tests/test_graph.py` — done when: `test_graph.py` passes (4-level order, diamond dedupe, depth-limit at 5 and at a per-agent override of 2, cycle detection, `extract()` miss-not-exception).

## Phase 4: Registry CRUD and per-agent enablement
- [x] T7 Implement `custom_apis.py` create/update/soft-delete: same-tenant validation of `upstream_api_id`, calls `_validate_credential_ref()` (T4) and `_validate_endpoint_url()` (T5), recomputes `chain_levels`/detects cycles for the edited row and every transitive dependent inside the write transaction under `pg_advisory_xact_lock(hashtext('custom_apis:' || tenant_id))`, refuses soft-delete while a live dependent exists, validates `success_template` placeholders against `sensitive_response_paths`/`sensitive` params — `services/toolexec/custom_apis.py`, `services/toolexec/tests/test_custom_apis.py` — done when: `test_custom_apis.py` passes (cross-tenant/nonexistent upstream rejection, same-name-two-tenants coexistence, 5-level chain and mid-graph edit rejection, cycle rejection, delete-while-dependent-live 409, literal-secret rejection, `success_template` sensitive/descendant/ancestor/param rejection cases and the PATCH-after-the-fact re-validation). Soft-delete does **not** additionally cascade-disable the API's `agent_custom_apis` rows: T11's ownership query joins `agent_custom_apis.custom_api_id = custom_apis.id` against a `custom_apis` row already filtered on `deleted_at IS NULL`, so once an API is soft-deleted no `agent_custom_apis` row referencing it can join regardless of its stale `enabled` flag — the join, not a cascade write, is what makes it unexecutable. State this explicitly in a code comment beside the soft-delete so the two halves read as one deliberate design, not an oversight.
- [x] T8 Implement `services/toolexec/agent_apis.py` `_authorize_agent_api()` (one query joining agent and API to the same tenant, 404 on missing/soft-deleted/wrong-tenant, before any write) and enable/disable logic rejecting enable when `api.chain_levels > effective max_chain_depth` — `services/toolexec/agent_apis.py`, `services/toolexec/tests/test_agent_apis.py` — done when: enabling a cross-tenant `custom_api_id` 404s with byte-identical detail to a random UUID and leaves `agent_custom_apis` row count unchanged; enabling an API whose `chain_levels` exceeds the agent's effective ceiling is rejected.

## Phase 5: Redaction and admission
- [x] T9 Implement `services/toolexec/redaction.py` `redact(payload, paths)` — done when: a unit test shows a path in `sensitive_response_paths` (and a `sensitive` param) render as `"[redacted]"` at any nesting depth while sibling fields are untouched.
- [x] T10 Implement `services/toolexec/admission.py` — per-`(tenant_id, agent_id)` concurrent-run cap (`TOOLEXEC_MAX_CONCURRENT_RUNS_PER_AGENT`, default 4) and per-minute cap (`TOOLEXEC_MAX_RUNS_PER_MINUTE_PER_AGENT`, default 60), in-process dicts swept inline, no background task — `services/toolexec/admission.py` — done when: a unit test drives `TOOLEXEC_MAX_CONCURRENT_RUNS_PER_AGENT + 1` concurrent `acquire()` calls for one `(tenant_id, agent_id)` and the last is refused while a different agent in the same tenant is admitted; a released slot admits a subsequent call.

## Phase 6: Executor (chain runner)
- [x] T11 Implement `services/toolexec/executor.py` steps 1–3: the single ownership-verification query —
  ```sql
  SELECT ca.*, atp.timeout_ms, atp.max_chain_depth
  FROM agents a
  JOIN custom_apis ca        ON ca.tenant_id = a.tenant_id AND lower(ca.name) = lower($3)
                             AND ca.deleted_at IS NULL
  JOIN agent_custom_apis aca ON aca.agent_id = a.id AND aca.custom_api_id = ca.id AND aca.enabled
  LEFT JOIN agent_tool_policies atp ON atp.agent_id = a.id AND atp.tool_name = 'execute_api'
                                   AND atp.enabled
  WHERE a.id = $2 AND a.tenant_id = $1 AND a.deleted_at IS NULL
  ```
  (`tenant_id`/`agent_id`/`api_name` all independently trusted-none; `ca.deleted_at IS NULL` and `a.deleted_at IS NULL` are load-bearing — this is what makes a soft-deleted API unexecutable the very next turn per lesson 16/verified-control 14, with no separate cascade write needed, see T7), `graph.resolve_order()` call with `min(request.max_chain_depth, 4)`, admission clamp (`chain_budget_ms` server-clamped to `TOOLEXEC_MAX_CHAIN_BUDGET_MS`) calling T10, and the run claim (`INSERT ... ON CONFLICT (tenant_id, idempotency_key) DO NOTHING RETURNING id` with the loser's path returning the recorded outcome or `chain_already_running`) — `services/toolexec/executor.py`, `services/toolexec/tests/test_chain_execution.py` — done when: a tenant-A `tenant_id` paired with a tenant-B `agent_id` returns `invalid_argument`/`api_not_enabled_for_agent` with zero HTTP calls and no `api_chain_runs` row; re-POST with the same run `idempotency_key` returns the recorded outcome with zero new transport calls; the `TOOLEXEC_MAX_CONCURRENT_RUNS_PER_AGENT + 1`th concurrent run returns `rate_limited` with no run row and zero transport calls; **a soft-deleted `custom_apis` row (with `agent_custom_apis.enabled` left stale `true`) is rejected `invalid_argument`/`api_not_enabled_for_agent` with zero HTTP calls** — the test that fails if the `ca.deleted_at IS NULL` clause is ever dropped from the query.
- [x] T12 Implement executor step 4 (per-step outbound call): calls `resolve_and_validate_endpoint()` (T5) before every call (not just at registration), constructs one `httpx.AsyncClient` per step with `transport=PinnedResolverTransport(allowed_ips)` (connects to the pre-validated address, leaves SNI/cert verification on the hostname, `verify=False` forbidden), `follow_redirects=False`, explicit `step_timeout_ms = min(api.timeout_ms or 6000, remaining budget)`, and abandons the response stream past `TOOLEXEC_MAX_RESPONSE_BYTES` (default 1 MiB, short-circuited on `Content-Length`) — `services/toolexec/executor.py` — done when: `test_chain_execution.py` cases pass for a step that hangs past its timeout (run `timeout`, no call made to the abandoned step's successor), a `302` returned as the step's result rather than followed, and a response-body-cap test asserting bytes actually pulled are at most the cap plus one chunk.
- [x] T13 Implement executor step 5 (argument resolution and placement): `literal`/`caller`/`upstream` sourcing via `graph.extract()`, type coercion, and placement rules — header values matched against `^[\x20-\x7E]*$` (CR/LF/control ⇒ `invalid_argument`/`illegal_header_value`, no request sent), path values as one `quote(value, safe="")` segment with raw `..` rejected, query values via `httpx`'s `params=`, body via `json=`/`data=` with no template substitution — `services/toolexec/executor.py` — done when: `test_chain_execution.py`'s injection cases pass (`"a\r\nAuthorization: Bearer x"` header value refused pre-request; `"../../admin/refund"` and `"x?admin=1"` path values produce a single percent-encoded segment under the registered path, asserted on the URL the transport received).
- [x] T14 Implement executor step 6 (side-effect fail-closed claim): the single `_derive(tag, tenant_id, custom_api_id, resolved_arguments)` HMAC function keyed by `TOOLEXEC_ARGS_HMAC_KEY_REF` (resolved once at startup via the platform resolver, absent ⇒ fail to start) and `TOOLEXEC_ARGS_HMAC_KEY_ID`; `arguments_hash = _derive("claim", ...)` stored, `idempotency_key = _derive("idem", ...)` exported only in the downstream header; the atomic `INSERT ... ON CONFLICT (tenant_id, custom_api_id, arguments_hash) DO UPDATE ... WHERE status='released' OR claimed_at < now() - $TTL RETURNING id` taken before the outbound call; release only on a non-409 4xx, keep on timeout/5xx — `services/toolexec/executor.py` — done when: `test_chain_execution.py` case 4b passes in full (concurrent same-session double-fire collapses to one outbound call; cross-session redial still refused; post-TTL claim at the **deployed default** `TOOLEXEC_SIDE_EFFECT_CLAIM_TTL` is taken over with history preserved; a differing argument is never gated; 422 releases while timeout/500 keep the claim; stored hash `!=` `sha256(args)`, differs under a second key ref, `!=` the exported `idem` value, and the `kid` prefix changes under a `kid` change; a NULL `arguments_hash` on a side-effecting step raises the `api_chain_steps_side_effect_keyed` CHECK). Also add the rotation caveat sentence — "a key rotation blinds both the platform claim and the downstream idempotency dedupe for one `TOOLEXEC_SIDE_EFFECT_CLAIM_TTL` window, since both derive from the same secret" — to the `_derive`/Rotation docstring in `executor.py`.
- [x] T15 Implement executor steps 7–8 (auth application, `success_template` interpolation, and persistence): `auth_schemes.apply()` resolves every ref through `resolve_tenant_ref()` (T4) at call time only, OAuth2 tokens cached in-process under `(tenant_id, custom_api_id)` with `expires_at - 60s` refresh and never persisted; each step persisted redacted (via T9) as it completes; run finalized `success`/`partial`/failing-status. **`success_template` interpolation sub-step (runtime half of control 30, only after a step succeeds and the run's final step is reached):** read the placeholder values **only** from the already-redacted response projection `redaction.redact()` produced (never the raw response), coerce each to `str`, strip control characters, truncate to 120 characters, and populate `ChainExecuteResponse.deterministic_response`; if any placeholder is unresolved (path absent from this response, or its value is `"[redacted]"`), leave `deterministic_response` as `None` entirely rather than speaking a partially-filled or literal `{{...}}` template — `services/toolexec/executor.py` — done when: an unresolvable/out-of-namespace credential ref yields `unavailable`/`credential_unavailable` with no outbound request and the ref absent from the error, the step row, and the log (asserted via `caplog`); a `sensitive` param and a `sensitive_response_paths` entry render `"[redacted]"` in both the step rows and the response while `api_name`/`status`/`argument_sources` stay visible; **and, per the test plan's `success_template` cases (f) and (g): a path made sensitive between registration and the call renders in the spoken text as `[redacted]`, not the underlying value, and a chain whose final step leaves one placeholder unresolved produces `deterministic_response is None` with the LLM narrating from `data` instead, asserted to contain no literal `{{`.**

## Phase 7: Routers and service wiring
- [x] T16 Implement `services/toolexec/routers/custom_apis.py` (`/tenants/{tenant_id}/custom-apis` + `/custom-apis/{id}` behind `get_current_user`/`require_role("superadmin","admin")` + `_require_tenant_access`) — `services/toolexec/routers/custom_apis.py` — done when: an authenticated `viewer` gets 403 on every write route **and gets 200 with the expected body on the `GET` list and detail routes** (reads are behind plain `get_current_user`, not the role gate), exercised end to end against T7 via `test_routes_auth.py`.
- [x] T17 Implement `services/toolexec/routers/agent_apis.py` (`/agents/{agent_id}/custom-apis` list/`PUT`/`DELETE`, all gated by `_authorize_agent_api()` from T8) — `services/toolexec/routers/agent_apis.py` — done when: `test_routes_auth.py`'s AC-10 write-time cases pass — a tenant-A admin `PUT`ing tenant B's `custom_api_id` onto their own agent gets 404 with byte-identical detail to a random UUID and `agent_custom_apis` gains no row; same for `DELETE` and for a tenant-B `agent_id`.
- [x] T18 Implement `_authorize_chain_runs(session_id, current_user)` in `services/toolexec/agent_apis.py` (the same module that owns `_authorize_agent_api`, per the design's chain-history-gate section) and `services/toolexec/routers/chain_runs.py` (`GET /calls/{session_id}/chain-runs`, gated by it) — `services/toolexec/agent_apis.py`, `services/toolexec/routers/chain_runs.py` — done when: `test_routes_auth.py`'s chain-history case passes — a tenant-A admin `GET`ing a tenant-B `session_id` gets 404 with detail byte-identical to an unknown `session_id`, and a platform-scoped caller sees the rows.
- [x] T19 Implement `services/toolexec/routers/execute.py` (`POST /internal/chains/execute` gated by `require_execute_subject()` reading `TOOLEXEC_EXECUTE_SUBJECTS`), wire all four routers plus this one into `app.py` — `services/toolexec/routers/execute.py`, `services/toolexec/app.py` — done when: `test_routes_auth.py`'s execute-identity cases pass — a `role="viewer"`, `tenant_id=NULL` service account not in `TOOLEXEC_EXECUTE_SUBJECTS` gets 403 (the vobiz/SDK case), a human `superadmin` gets 403, the allow-listed Conversation account succeeds, and every route 401s with no auth header.

## Phase 8: Conversation-side integration
- [x] T20 Add the `_EXECUTE_API` `ToolDefinition` to `services/conversation/tools/registry.py` and add `max_chain_depth`/`sensitive_arg_keys` to `ResolvedToolPolicy` plus `_specialize_execute_api()` in `services/conversation/tools/policy_resolver.py` (the tenant-fenced enabled-APIs query joining `agents.tenant_id = custom_apis.tenant_id`) — `services/conversation/tools/registry.py`, `services/conversation/tools/policy_resolver.py` — done when: `test_execute_api_tool.py`'s registry tripwire (`{book_appointment, cancel_appointment, reschedule_appointment, send_sms, execute_api}`) passes, an agent with 7 enabled APIs gets exactly one `execute_api` entry with a 7-value enum, an agent with zero enabled APIs gets no `execute_api` entry and issues no second query, and `test_policy_resolver.py` asserts `sensitive_arg_keys == {"national_id"}` for an agent with one API carrying that `sensitive` param.
- [x] T21 Add `redact_arg_keys` to `LoggingMiddleware.__init__` and `build_default_chain()` in `services/conversation/tools/middleware.py`, and pass `policy.sensitive_arg_keys` at the `build_default_chain` call site in `services/conversation/tools/orchestrator.py` — `services/conversation/tools/middleware.py`, `services/conversation/tools/orchestrator.py` — done when: `test_execute_api_tool.py`'s `caplog`-driven logging cases pass against the real `build_default_chain` — a sentinel value under a key in `sensitive_arg_keys` appears in no `caplog.records` message or args, `api_name` and non-sensitive inputs still appear, and the empty-`frozenset()` default logs byte-identical to today.
- [x] T22 Implement `services/conversation/tools/provider_manager.py` `_make_toolexec` (`engine='toolexec'`, no `api_key_ref` required) and `services/conversation/tools/providers/toolexec/client.py` `ToolExecClient` (lazy service-account login, one re-auth on 401, explicit `httpx` timeout) — `services/conversation/tools/provider_manager.py`, `services/conversation/tools/providers/toolexec/client.py` — done when: constructing the provider for a `tool_provider_config` with `engine='toolexec'` and no `api_key_ref` succeeds, and a unit test forces a 401 and confirms exactly one re-auth attempt.
- [x] T23 Implement `services/conversation/tools/executors/api_exec_executor.py` `ApiExecExecutor` (derives `chain_budget_ms` from `request.context.deadline`, posts once, maps `chain_status`→`ToolStatus` including `partial`→`FAILED` with `payload["partial"]=True`, returns only the redacted `data` projection) and register `execute_api` in `services/conversation/__main__.py`'s `ExecutorRegistry` reading `TOOLEXEC_SERVICE_URL` — `services/conversation/tools/executors/api_exec_executor.py`, `services/conversation/__main__.py` — done when: `test_execute_api_tool.py` passes the `chain_status`→`ToolStatus` mapping table and the barge-in case (`cancel_event` set mid-call yields `FAILED/"cancelled"` and the abandoned task's result is never folded into history).
- [x] T24 Exempt `engine='toolexec'` from the `api_key_ref or api_key is required` check in `services/config/routers/tool_provider_configs.py` — `services/config/routers/tool_provider_configs.py` — done when: creating a `tool_provider_config` with `engine='toolexec'` and no `api_key_ref` succeeds, and every other engine still 400s without one.

## Phase 9: Admin UI
- [x] T25 Add `admin-ui/lib/toolexecApi.ts` (typed client against `NEXT_PUBLIC_TOOLEXEC_SERVICE_URL`, default `http://localhost:8600`, following `knowledgeApi.ts`'s pattern) — `admin-ui/lib/toolexecApi.ts` — done when: the client's CRUD/enablement/chain-history calls compile against `services/toolexec/schemas.py`'s shapes and a smoke call against a running service returns 200.
- [x] T26 Add `admin-ui/components/CustomApisPanel.tsx` (list/create/edit an API — name, description, endpoint, method, auth scheme + ref, params with `source`, sensitive flags — per-agent enable toggle, `execute_api` master switch) — `admin-ui/components/CustomApisPanel.tsx` — done when: a viewer can render the list (per lesson 21, its sibling write-scoped fetches failing independently must not blank the panel) and an admin can create, enable-for-agent, and soft-delete an API through the UI against a local `services/toolexec` instance.
- [x] T27 Add `admin-ui/components/KnowledgeBaseTabs.tsx` (Documents → existing `<KnowledgeBasePanel/>` verbatim, APIs → `<CustomApisPanel/>`) and switch `admin-ui/app/agents/[tenantSlug]/[agentSlug]/page.tsx` line ~794 to render it — `admin-ui/components/KnowledgeBaseTabs.tsx`, `admin-ui/app/agents/[tenantSlug]/[agentSlug]/page.tsx` — done when: the agent detail page renders both sub-tabs, `KnowledgeBasePanel.tsx` itself is unchanged (diff confirms it), and no other reference to the old direct `<KnowledgeBasePanel/>` render remains at that call site.

## Phase 10: Operations and documentation
- [x] T28 Add `start_toolexec_service()` to `scripts/start_local.sh`, add port 8600 to the `verify` port loop, health curl and `portmap` line — `scripts/start_local.sh` — done when: `scripts/start_local.sh` starts, health-checks, and lists `services/toolexec` alongside the other services, and every hint branch still `return 0`s (lesson 15).
- [x] T29 Document every new env var in `docs/setup.md` (`TOOLEXEC_SERVICE_URL`, `TOOLEXEC_EXECUTE_SUBJECTS`, `TOOLEXEC_TENANT_SECRET_ROOT`, `TOOLEXEC_HTTP_HOST_ALLOWLIST`, `TOOLEXEC_ARGS_HMAC_KEY_REF`/`_KEY_ID`, `TOOLEXEC_SIDE_EFFECT_CLAIM_TTL`, `TOOLEXEC_MAX_CHAIN_BUDGET_MS`, the two run caps, `TOOLEXEC_MAX_RESPONSE_BYTES`, service-account creds) and the new service in the start order; add one row to `CURSOR.md`'s area table (Custom API chains → `services/toolexec/`) — `docs/setup.md`, `CURSOR.md` — done when: the `TOOLEXEC_ARGS_HMAC_KEY_REF` rotation row states plainly that rotating the key blinds **both** the platform side-effect claim and the downstream API's own idempotency dedupe for one `TOOLEXEC_SIDE_EFFECT_CLAIM_TTL` window, since `_derive("idem", ...)` uses the same rotated key — matching the sentence added to `executor.py` in T14.

## Phase 11: Manual verification
- [ ] T30 Run the full stack via `scripts/start_local.sh`, drive the Knowledge Base → APIs sub-tab as both `admin` and `viewer`, register a real 3-level chain against a local stub, place a webcall, and confirm the agent narrates level-1 lookup + level-3 action then speaks no success on an injected failure path — done when: all of the above is observed live in a browser (lesson 23), including the viewer's APIs sub-tab still rendering its list per lesson 21.
