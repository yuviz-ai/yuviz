# Tasks: Unified Telephony Provider Service (`services/telephony/`)

## Phase 1: Provider SDK extensions

- [x] T1 Add `NormalizedInboundCall`/`ReconcileResult` dataclasses, the abstract `normalize_inbound_webhook`, and concrete defaults `sensitive_credential_fields()`/`check_health()`/`transfer_call()`/`reconcile_call()` plus the `ISmsProvider` ABC — `libs/telephony_sdk/interface.py` — done when: a unit test subclassing `ITelephonyProvider` without overriding `normalize_inbound_webhook` raises `TypeError: abstract`, and a minimal subclass that does implement it instantiates cleanly with the concrete defaults returning `[]`/`True`/raise/`"indeterminate"` respectively.
- [x] T2 Add `register(name, cls, *, hidden=False)`, `visible()`, and a parallel `SmsProviderRegistry` — `libs/telephony_sdk/registry.py` — done when: a unit test registers a provider with `hidden=True`, confirms it is absent from `visible()`, and confirms `get(name)` still resolves it.
- [x] T3 Implement `normalize_inbound_webhook` (form `CallUUID`/`To`/`From`, `known_tenant_slug=None`), `sensitive_credential_fields() -> ["auth_token"]`, `check_health()`, and add `ISmsProvider` bases with `send_sms`/`get_message_status` — `libs/telephony_sdk/providers/vobiz.py` — done when: `VobizProvider` instantiates without `TypeError: abstract` and a unit test parses a sample Vobiz webhook form payload into the correct `NormalizedInboundCall`.
- [x] T4 Implement `normalize_inbound_webhook` (query ∪ JSON-or-form, `CallSid`/`To`/`From`, `known_tenant_slug=account_tenant_slug`, domain mismatch raises `WebhookRejected`); replace `verify_webhook_signature`'s hardcoded `return False` with an all-entries `hmac.compare_digest` over `self._api_keys`, skipping any `is_encrypted()` entry; add `sensitive_credential_fields() -> ["api_keys"]`, `check_health()` — `libs/telephony_sdk/providers/cloudonix.py` — done when: a unit test shows `verify_webhook_signature` returns `True` only for a matching plaintext key, `False` for a wrong key, and `False` (not a match) when the stored key is sealed via `is_encrypted()`.
- [x] T5 Add `FakeProvider(ITelephonyProvider, ISmsProvider)` with deterministic ids and scriptable failures/timeouts/`check_health` results, registered `hidden=True` under `"fake"` in both registries; import it from `libs/telephony_sdk/providers/__init__.py` — `libs/telephony_sdk/providers/fake.py` (new), `libs/telephony_sdk/providers/__init__.py` — done when: `TelephonyProviderRegistry.get("fake")` and `SmsProviderRegistry.get("fake")` both resolve, and `"fake" not in visible()` for both registries.
- [x] T6 Add `test_interface_conformance.py`, `test_transfer_unsupported.py`, `test_check_health_default.py` — `libs/telephony_sdk/tests/` — done when: `pytest libs/telephony_sdk/tests/` passes, including an assertion that the registry's enumerated size equals the count of provider modules under `providers/` (so a non-conforming addition fails the enumeration instead of being skipped).

## Phase 2: Telephony service core

- [x] T7 Add the package skeleton mirroring `services/cloudonix/__main__.py`'s shape — `services/telephony/__init__.py` (new), `services/telephony/__main__.py` (new) — done when: `python -m services.telephony` imports and starts uvicorn bound to port 8750 by default without a `ModuleNotFoundError` on the transitive `conversation_pb2_grpc` import.
- [x] T8 Add the generalized `AccountStore` (moved from `services/cloudonix/accounts.py`), keyed `(provider, account_ref)` and `tenant_slug → default-outbound account`, decrypting every field named by `sensitive_credential_fields()`, prewarming the `(tenant_slug, agent_slug)` ownership memo on preload and each 300s refresh — `services/telephony/accounts.py` (new) — done when: `test_accounts.py` passes: a scalar and a list-valued sensitive field both decrypt, a plaintext legacy value passes through with a warning, an undecryptable entry is skipped without failing the refresh, and a failed Config fetch keeps the last-known-good map.
- [x] T9 Add `require_telephony_caller` (admits `superadmin`/`admin` console roles or a NULL-tenant service account, rejects a tenant-scoped `viewer`) and the `async def resolve_caller_tenant` that maps `tenant_slug` to a UUID via the `AccountStore` map and `await`s `deps.assert_tenant_access` — `services/telephony/auth.py` (new) — done when: a unit test shows console admin and NULL-tenant service-account callers pass, a tenant-scoped `viewer` gets 403, and `resolve_caller_tenant` is itself `async def` with the awaited call visible in its body.
- [x] T10 Add `resolve_outbound_identity()` — validates the caller-id DID via `did:{did}` and the agent via the memo/Redis/cold-Config chain, both against the authenticated tenant, before any vendor call — `services/telephony/ownership.py` (new) — done when: a unit test shows a foreign-tenant DID, a foreign-tenant agent, and an unknown agent (memo miss + Redis miss + Config 404) each raise `OwnershipError`, while the tenant's own DID/agent pair returns an `OutboundIdentity`.
- [x] T11 Add `CallContextStore` (moved from `services/cloudonix/handoff.py` as-is: server-minted `secrets.token_urlsafe(32)`, claim-once, TTL, capacity bound), with `CallRoute` gaining `provider`/`account_ref`/`provider_call_id` — `services/telephony/callctx.py` (new) — done when: `test_callctx.py` passes: the admission token is not derivable from `provider_call_id`, connecting with the `provider_call_id` in the path is refused (1008), a second connection with the same valid token is refused, and a token issued under account A does not admit a connection claiming account B.
- [x] T12 Add `claim()`/`finalize()`/`await_outcome()`/`note_reference()`/`observed_call_id()`/`read()` over the `idem:{provider}:{tenant_id}:{key}`/`idemref:{...}` keys, `tenant_id` required and positional-second on every function through one private `_key()` helper — `services/telephony/idempotency.py` (new) — done when: `test_idempotency.py` passes against a real Redis: a concurrent second call to a finalized key returns the cached body without touching `FakeProvider`; a second call to a never-finalized key returns 202 within a wall-clock window bounded both above and below `POLL_BUDGET_S=10.0`; timeout+`reconcile=placed`/`not_placed`/`indeterminate` produce 200-with-id / `ok:false` / 202 respectively.
- [x] T13 Add `handle_inbound_webhook()` implementing the fixed order (registry lookup → loaded check → account-bucket rate limit → signature → normalize → DID-bucket rate limit → route resolution → context issue → answer response) and `resolve_inbound_route()` (tenant from `known_tenant_slug`/`account.tenant_slug`, never the DID; a DID hit whose tenant differs from the account's tenant is a 403) — `services/telephony/orchestrator.py` (new) — done when: `test_ratelimit_order.py` shows N+1 all-invalid-signature requests get 429 (not 403), and `test_inbound_orchestration.py` passes its five cases (valid signature → server-minted stream URL + one stored context; invalid signature → 403 with empty `CallContextStore` and `resolve_did_route` never called; unknown provider → 404 with zero rate-limiter increments; foreign-DID normalized call → 403; an AST check that neither provider adapter references `did_route` or `CallContextStore`).
- [x] T14 Add `place_call()`/`send_sms()` implementing the four deterministic `OutboundResult` outcomes (claim-won-answered, claim-lost-cached, claim-lost-or-timeout-indeterminate → 202, claim-won-timeout-reconciled) against a provider resolved server-side from `identity` — `services/telephony/outbound.py` (new) — done when: a unit test against `FakeProvider` exercises all four outcomes and confirms neither function accepts a raw `tenant_slug`/`agent_slug`/`from_number` — only `OutboundIdentity`.
- [x] T15 Add `health_loop()` — every `TELEPHONY_HEALTH_INTERVAL_S` (default 300) probe every loaded account with a 5s per-probe timeout and bounded concurrency, write `telephony:health:{config_id}` with `EX 900`, running only in its own asyncio task — `services/telephony/health.py` (new) — done when: `test_health.py` passes: probe sequences `[]`/`[ok]`/`[ok,fail]`/`[fail,fail]` map to standby/healthy/degraded/degraded, and the written key's TTL exceeds the interval.
- [x] T16 Add the FastAPI app, `lifespan` (account preload, refresh task, health task, both cancelled on exit), the inbound (`/{provider}/voice/{account_ref}`, `/{provider}/stream/{call_uuid}`, `/{provider}/status/{account_ref}`) and outbound (`/{provider}/call`, `/{provider}/call/idempotency/{key}`, `/sms/send`) routes wired to `orchestrator`/`auth`/`ownership`/`outbound`/`idempotency`/`callctx`, and an undepended `/health` — `services/telephony/app.py` (new) — done when: the app starts under `uvicorn` locally, `GET /health` returns `{"loaded": ...}` with no dependency injected, and a manual `curl` through each route reaches the module it's documented to call.
- [x] T17 Add the outbound-auth regression suite, each case written to fail red if its control is removed — `services/telephony/tests/test_outbound_auth.py` (new), `services/telephony/tests/test_outbound_roles.py` (new) — done when: both pass against the live `app.routes`-walked enumeration (asserting the walk's own size, not just its members): no-auth → 401 with zero `FakeProvider` dial/send calls; cross-tenant `tenant_slug` in body → 403/404 with an invariant response shape; foreign-tenant `from`/`agent_slug` → 403; two tenants racing the identical idempotency key each get their own result; a stale idempotency read for another tenant's key → 404 indistinguishable from never-existed; a tenant `viewer` → 403 while a NULL-tenant service account with the same `role="viewer"` claim passes.
- [x] T18 Add the hot-path-isolation, status-callback-scoping, and unawaited-guard regression suite — `services/telephony/tests/test_hot_path_isolation.py` (new), `services/telephony/tests/test_status_callback_scoping.py` (new), `services/telephony/tests/test_no_unawaited_guards.py` (new) — done when: `test_hot_path_isolation.py` shows the WS and inbound voice/status routes have empty `dependant.dependencies`, an AST check finds no `auth`/`ownership`/`idempotency` import in the WS handler's module, and a connected-call fixture streams against a Redis client that raises on every call; `test_status_callback_scoping.py` shows a callback signed by account A carrying tenant B's idempotency key writes `idemref` only under A's tenant; `test_no_unawaited_guards.py`'s AST walk over every `services/telephony/` module finds a non-zero, all-awaited set of calls to `assert_tenant_access`/`resolve_caller_tenant`/`resolve_outbound_identity` and fails on any bare (un-awaited) call or a non-`async def` definition of the latter two.
- [x] T19 Add a `telephony` service block (own `TELEPHONY_BIND_ADDR`/`TELEPHONY_PORT`, default `127.0.0.1:8750`, copying the `cloudonix` block's env + healthcheck + `depends_on: conversation`) — `deployment/docker/docker-compose.yml` — done when: `docker compose config` validates the new block and its healthcheck targets `/health`.

## Phase 3: Config service integration

- [x] T20 Make `_normalize_credentials()` provider-agnostic over `sensitive_credential_fields()` (scalar and list-valued), add `_NON_REST_PROVIDERS = {"native"}` early-return on normalize and validate, rebuild `list_supported_providers()` from `TelephonyProviderRegistry.visible()`, attach a per-row `health` dict to `list_telephony_configs()` via one `cache.get_json` per row — `services/config/telephony_configs.py` — done when: extended `services/config/tests/test_telephony_configs.py` passes: Vobiz's `auth_token` and Cloudonix's `api_keys` list both seal to `enc:`; an already-`enc:` value round-trips un-double-encrypted; an `env:`/`k8s:` value is rejected for both a scalar and a list field; `provider='native'` skips normalize/validate; `list_supported_providers()` excludes `"fake"` and exposes `{required, sensitive}` per provider.

## Phase 4: Campaigns generalization

- [x] T21 Add `originate_call()`/`poll_idempotency()` posting to `{TELEPHONY_SERVICE_URL}/{provider}/call` with the Config service-account JWT (same login/401-retry-once helper `services/vobiz/app.py:62-88` uses), 3 retries with 0.5/1/2s backoff on `httpx.HTTPError`/5xx reusing the same key, raising `TelephonyOriginatePending` on 202 — `services/campaigns/telephony_originate.py` (new, replaces `vobiz_originate.py`) — done when: a unit test against a mock telephony endpoint shows the retry/backoff behavior reuses one key across attempts and a 202 raises `TelephonyOriginatePending` carrying the key.
- [x] T22 Mint `sha256(campaign_id:contact_id:attempt_count)` from the row `claim_next_pending()` returns; dispatch `provider in TelephonyProviderRegistry.all()` → REST via `telephony_originate`, else ESL; track 202s in `_pending_idem` and resolve on a later tick instead of requeueing as failed — `services/campaigns/worker.py` — done when: extended `services/campaigns/tests/test_worker.py` passes: the same attempt retried through the HTTP hop reuses one key across 3 attempts; a requeued attempt (incremented `attempt_count`) mints a different key; a 202 leaves the contact at `'calling'` and resolves via `poll_idempotency` on the next tick; a `'native'`/`None` provider still takes the ESL path.

## Phase 5: Migration, dashboards, and docs

- [x] T23 Add the one-off, re-runnable migration: relabel 5000-5009 `'cloudonix'`→`'native'` rows (no other column touched, `updated_at` untouched), seal pre-existing plaintext sensitive credential fields via `telephony_configs._normalize_credentials()`, and `DEL telephony_config:{id}` for every relabelled row — `scripts/migrate_telephony_providers.py` (new) — done when: run against a DB seeded with a 5000-5009 `'cloudonix'` row, a non-5000-range `'cloudonix'` row, and an already-`'native'` row, only the first changes, `updated_at` is byte-identical before/after, and a second run reports zero rows.
- [x] T24 Type `TelephonyConfig.health` and retype `listTelephonyProviders`; replace the DID-count heuristic at line 305 with `config.health?.status ?? "standby"` — `admin-ui/lib/api.ts`, `admin-ui/app/telephony/page.tsx` — done when: the Telephony page's health badge renders from the API's `health.status` field in a browser, not from a DID count.
- [x] T25 Add the telephony service's start block (env vars, port 8750 in the port list and legend, hint branches `return 0`) — `scripts/start_local.sh` — done when: sourcing `scripts/start_local.sh` starts `services.telephony` alongside the other services and lists 8750 in the printed legend without killing the shell on any hint branch.
- [x] T26 Write the route table, env vars, and cutover runbook, replacing `docs/cloudonix.md`'s role — `docs/telephony.md` (new) — done when: the doc enumerates every inbound/outbound route from the design's route table, the `TELEPHONY_*` env vars, and the phase-1/2/3 cutover steps including the vendor dashboard URL change.

## Phase 6: Decommission

- [x] T27 Delete `services/vobiz/`, `services/cloudonix/`, `services/campaigns/vobiz_originate.py`, `docs/cloudonix.md`, their `docker-compose.yml` services and `start_local.sh` blocks; update `CURSOR.md`'s service table to the single unified telephony row — `deployment/docker/docker-compose.yml`, `scripts/start_local.sh`, `CURSOR.md` (plus the deletions listed) — done when: `grep -rn 'services\.vobiz\|services\.cloudonix\|8500\|8700' services/ admin-ui/ scripts/ docs/ deployment/` returns nothing.
