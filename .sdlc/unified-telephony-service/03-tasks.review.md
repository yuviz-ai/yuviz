# Review: 03-tasks.md (Unified Telephony Provider Service)
VERDICT: AMBER

1. [blocking] T20's "done when" tests only credential normalization and `list_supported_providers()` — it never asserts that `list_telephony_configs()` attaches the per-row `health` dict (AC21-24/AC28), even though T20's own task description names this and T24 (admin-ui badge) is built entirely on that field existing — services/config/telephony_configs.py task, T20 in `.sdlc/unified-telephony-service/03-tasks.md`. Fix: add a done-when case to T20 asserting `list_telephony_configs()` returns `health` read via `cache.get_json` per row (present → status/checked_at, absent → `{"status": "standby", "checked_at": None}`), and that it is not folded into the 60s-cached `telephony_config:{id}` row.

2. [blocking] The design's Risks section states the credential-sealing statement in `migrate_telephony_providers.py` must run "in phase 1, *after* Vobiz is repointed" — sealing it earlier breaks the still-running old `services/vobiz`, which reads plaintext credentials directly. T23 places this entire script (relabel + seal) in Phase 5, after Campaigns generalization (Phase 4), with no note gating its execution on Vobiz's cutover having already happened — T23 in `.sdlc/unified-telephony-service/03-tasks.md`. Fix: add an explicit precondition to T23's done-when ("run only after Vobiz's webhook URLs are repointed to :8750 / services/vobiz is retired") or split the sealing statement into its own task sequenced right after the service-deployment task, matching the design's stated ordering.

3. [minor] T13's done-when covers 4 of orchestrator's fixed-order steps (rate-limit-first, invalid-sig, unknown-provider, foreign-DID) but never exercises step 2, `accounts.loaded == False ⇒ 503` ("keeps today's no-key-map-means-admit-nothing posture") — `.sdlc/unified-telephony-service/03-tasks.md` T13. Fix: add a case asserting a pre-preload request returns 503 before touching the rate limiter.
