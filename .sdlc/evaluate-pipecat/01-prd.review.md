# Review: 01-prd.md
VERDICT: AMBER

1. [blocking] The "How this is solved elsewhere" section pre-bakes the evaluation's own conclusion before any evaluation happens — it states Dograh "is not an adoption option," recommends against full replacement, and asserts the Kamailio/hot-path layer is definitively harder than Pipecat's gap, all inside the PRD that is supposed to define what the evaluator must independently determine (AC5 requires the evaluator produce "a single explicit statement" of the decision). A PRD that already contains the answer turns the downstream evaluation into confirmation work, and every claim feeding that pre-baked conclusion is explicitly marked unverified — .sdlc/evaluate-pipecat/01-prd.md lines 70-89 — fix: keep the market-research section as background/context only ("options that exist," "known gap reported by X"), move the "recommendation going in" framing out of the PRD or label it clearly as a non-binding hypothesis the evaluator must independently confirm or refute, not the intro's stated position.
2. [minor] AC4 says every doc/issue/article-sourced claim must be "unverified" unless the bounded scratch smoke test confirmed it, but the PRD's own market-research section states several claims as flat fact with only a parenthetical "(unverified...)" tag rather than inline per-claim attribution to a named source (e.g. "Documented Pipecat production issues as of 2026... include 4-5s cold-start..." — no specific source named for each of the three numbers) — .sdlc/evaluate-pipecat/01-prd.md lines 66-69 — fix: if this pattern is meant as the template for the eventual deliverable, tighten it to name the specific source per claim (issue number, article, docs page) rather than one shared "(unverified beyond the cited sources)" covering three distinct numbers.
3. [minor] The Dograh "Vobiz" provider-name coincidence (line 48) is flagged as worth investigating but given real evaluative weight ("unlikely to be pure coincidence") without any grep/check against this codebase's own `services/vobiz` for a shared upstream API — this is speculation presented with a confidence level the PRD elsewhere requires evidence for — fix: soften to a neutral "check and report" instruction with no editorializing on likelihood, consistent with the "unverified by default" stance the rest of the PRD holds itself to.
</content>
